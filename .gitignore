venv/
downloads/
__pycache__/
*.pyc
assets/*.mov
.DS_Store
.env
.env.local
.env.production
.env.development
# site-specific embed providers (real domains stay out of the repo)
providers.json

# runtime provider config (real domains live here, never commit)
config/

# locally installed MEGA-enabled rclone binary
.tools/
